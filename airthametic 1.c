#include <stdio.h>
main()
{
	int n,a,l,s;
	printf("/nenter the a");
	printf("/nenter the l");
	printf("/nenter the n");
	scanf("%d%d%d",&n,&a,&l);
	s=n*(a+l)/2;
	printf("s value is %d",s);
}
